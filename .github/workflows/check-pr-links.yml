name: Check Links in Pull Requests

on:
  pull_request:
    branches:
      - main
    paths:
      - '**/*.md'

jobs:
  check-links:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ Get changed Markdown files
      - name: Get changed Markdown files
        id: changed-files
        run: |
          CHANGED_FILES=$(git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.sha }} | grep '\.md$' || true)
          CHANGED_FILES="${CHANGED_FILES//$'\n'/ }"   # replace newlines with spaces
          echo "CHANGED_FILES=$CHANGED_FILES" >> $GITHUB_ENV
          echo "Changed Markdown files: $CHANGED_FILES"

      # 3️⃣ Skip if no Markdown files changed
      - name: Skip if no Markdown files changed
        if: env.CHANGED_FILES == ''
        run: |
          echo "No Markdown files changed. Skipping link check."
          exit 0

      # 4️⃣ Run Lychee in checkstyle format
      - name: Run Lychee
        id: run-lychee
        run: |
          mkdir -p lychee
          lychee ${{ env.CHANGED_FILES }} \
            --include-fragments \
            --no-progress \
            --format checkstyle \
            > lychee/checkstyle.xml
          cat lychee/checkstyle.xml

      # 5️⃣ Annotate PR inline using Reviewdog
      - name: Annotate broken links inline
        uses: reviewdog/action-lychee@v1
        with:
          name: lychee
          reporter: github-pr-review
          level: error
          input: lychee/checkstyle.xml
          fail_on_error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
