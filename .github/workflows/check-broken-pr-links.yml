name: Check Links in Pull Requests

on:
  pull_request:
    branches:
      - main
    paths:
      - '**/*.md'

jobs:
  check-links:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ Get changed Markdown files in the PR
      - name: Get changed Markdown files
        id: changed-files
        run: |
          CHANGED_FILES=$(git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.sha }} | grep '\.md$' || true)
          CHANGED_FILES="${CHANGED_FILES//$'\n'/ }"   # replace newlines with spaces
          echo "CHANGED_FILES=$CHANGED_FILES" >> $GITHUB_ENV
          echo "Changed Markdown files: $CHANGED_FILES"

      # 3️⃣ Skip if no Markdown files changed
      - name: Skip if no changed Markdown files
        if: env.CHANGED_FILES == ''
        run: |
          echo "No Markdown files changed. Skipping link check."
          exit 0

      # 4️⃣ Run Lychee on changed files (compact output)
      - name: Run Lychee
        id: run-lychee
        uses: lycheeverse/lychee-action@v2
        with:
          args: |
            --no-progress
            --include-fragments
            --format detailed
            ${{ env.CHANGED_FILES }}
          output: lychee/out_raw.md
          fail: false   # ✅ don't fail yet, let us capture output

      # 5️⃣ Clean Lychee output (remove summary line + mark if has content)
      - name: Format Lychee output for PR comment
        id: format-output
        if: always()
        run: |
          mkdir -p lychee

          # Start the comment
          echo "🚨 **Link Checker found issues in this PR**" > lychee/comment.md
          echo "" >> lychee/comment.md
      
          # Process Lychee compact output to be user-friendly
          awk '
            /^Errors in / {
              file=$3
              print "\n**File:** " file >> "lychee/comment.md"
              next
            }
          
            /\[ERROR\]/ {
              match($0, /\[ERROR\] ([^|]+)( \|.*line:([0-9]+))?/, m)
              msg=m[1]
              line = (m[3] ? m[3] : "?")
              print "- ⚓ Anchor not found → " msg " (line " line ")" >> "lychee/comment.md"
              next
            }
          
            /\[404\]/ {
              match($0, /\[404\] ([^|]+)( \|.*line:([0-9]+))?/, m)
              url=m[1]
              line = (m[3] ? m[3] : "?")
              print "- ❌ 404 Not Found → " url " (line " line ")" >> "lychee/comment.md"
              next
            }
          
            /\[301\]|\[302\]/ {
              match($0, /\[(301|302)\] ([^|]+)( \|.*line:([0-9]+))?/, m)
              code=m[1]; url=m[2]
              line = (m[4] ? m[4] : "?")
              print "- 🔀 Redirected (" code ") → " url " (line " line ")" >> "lychee/comment.md"
              next
            }
          
            /Timeout/ {
              match($0, /Timeout ([^|]+)( \|.*line:([0-9]+))?/, m)
              url=m[1]
              line = (m[3] ? m[3] : "?")
              print "- ⏳ Timeout → " url " (line " line ")" >> "lychee/comment.md"
              next
            }
          ' lychee/out_raw.md


      # 6️⃣ Comment broken links on PR
      - name: Comment broken links
        if: always() && (env.CHANGED_FILES != '') && (steps.format-output.outputs.has_content == 'true')
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          path: lychee/comment.md
          recreate: true
          
      # 7️⃣ Fail workflow if broken links are found
      - name: Fail workflow if broken links
        if: steps.format-output.outputs.has_content == 'true'
        run: |
          echo "❌ Broken links detected. Please review the PR comment for details."
          exit 1
